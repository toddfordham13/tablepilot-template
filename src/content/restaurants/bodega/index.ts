import { config } from "./config"
import { gallery } from "./gallery"
import { hero } from "./hero"
import { menu } from "./menu"

export const bodegaRestaurant = {
  config,
  hero,
  menu,
  gallery,
}